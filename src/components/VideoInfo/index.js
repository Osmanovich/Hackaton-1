import { register } from "@frukmruk/html";

const VideoInfo = () => {
  return (
    <div id="video-info" class="w-full h-10 p-2 text-gray-500 text-sm">
      #Video-info
    </div>
  );
};

register({ VideoInfo });

export default VideoInfo;
